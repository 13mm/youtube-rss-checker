name: YouTube RSS Checker

permissions:
  contents: write
  
on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Run script
        env:
          WEBHOOK_GAME: ${{ secrets.WEBHOOK_GAME }}
          WEBHOOK_MUSIC: ${{ secrets.WEBHOOK_MUSIC }}
          WEBHOOK_VLOG: ${{ secrets.WEBHOOK_VLOG }}
          WEBHOOK_OTHER: ${{ secrets.WEBHOOK_OTHER }}
        run: node checkYoutube.js

      - name: Save seen.json
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add data/seen.json
          git commit -m "Update seen.json" || echo "No changes"
          git push
